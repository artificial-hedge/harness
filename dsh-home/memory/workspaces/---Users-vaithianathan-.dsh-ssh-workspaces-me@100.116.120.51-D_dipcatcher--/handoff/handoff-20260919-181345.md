# 交接账本 · 2026-09-19 18:13

任务状态：量化研究/纸面交易质量门槛仍未完成；import blockers 正在并行修复。
目标：恢复 covariance、realized_garch、robinhood_plus API，保留 research-only 约束，并以可复现命令证明质量。
已完成：`paper/__init__.py` 改为惰性导出；`validate_ledger_schema(..., require_complete=True)` 对缺失 broker/equity/orders/cash/analytics fail-closed；直接 smoke 通过。方案→完整 pytest 仍因 covariance 缺失导入失败。
下一步：等待并审查三个 agent 的模块实现，先运行 covariance/DCC/realized/robinhood focused tests，再回归 ledger/paper tests；首个命令：`uv run pytest -q tests/unit/test_covariance.py tests/unit/test_dcc.py`。